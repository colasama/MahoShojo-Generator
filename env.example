# 修改为 .env.local 来使用捏
# OpenAI API 密钥数组（用逗号分隔）
# 支持多个 API Key，当第一个失败时会自动尝试其他的
NEXT_PUBLIC_AI_API_KEYS=your_openai_api_key_here,your_backup_api_key

# API 基础 URL 数组（用逗号分隔，与 API_KEYS 一一对应）
# 如果某个 API Key 没有对应的 URL，将使用 OpenAI 默认地址
NEXT_PUBLIC_AI_BASE_URLS=https://api.openai.com/v1,https://api.backup-provider.com/v1

# 使用的模型（可选，默认 gpt-3.5-turbo）
NEXT_PUBLIC_AI_MODEL=gpt-3.5-turbo

# 其他可选配置
# NEXT_PUBLIC_AI_TEMPERATURE=0.8
# NEXT_PUBLIC_AI_MAX_TOKENS=1000

# 向后兼容：如果只有单个 API Key，也可以使用旧格式
# NEXT_PUBLIC_AI_API_KEY=your_openai_api_key_here
# NEXT_PUBLIC_AI_BASE_URL=https://api.openai.com/v1 