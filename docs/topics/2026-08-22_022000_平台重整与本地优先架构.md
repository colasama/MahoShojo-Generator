# 平台重整与本地优先架构主题

2026-09-12：维护者确认 Phase 2.5 与 Arena 多人已完成，正式开始 [独立线上管理端业务迁移](../plans/2026-09-12_130000_独立线上管理端迁移实施计划.md)。本页较早执行窗口是历史记录；G3-P0 临时禁令不再阻止已授权的业务迁移。`homura-admin.colanns.me` 属于 Arcane Docker 管理，新业务管理端使用 `admin.mahoshojo.colanns.me`。

Admin 当前功能去向、迁移影响和验证见 [交付记录](../migration/2026-09-12_144000_独立管理端功能迁移与交付记录.md)；生产配置与恢复见 [部署验收 runbook](../runbooks/2026-09-12_143000_独立管理端部署与恢复验收.md)。代码完成不等于生产激活。

更新时间：2026-09-12
状态：`active`
主题标识：`platform-restructure-local-first`

## 权威性说明

本页是 MahoShojo-Generator 平台重整工作的稳定入口。

2026-09-01 起，[平台复杂度预算与故障降级决策](../decisions/2026-09-01_193200_平台复杂度预算与故障降级决策.md)
覆盖与其冲突的 source digest、源码 hash、evidence registry 和历史审计 merge gate 口径。真实认证授权、secret、
资源上限、Provider 防重复 dispatch 与运行行为测试继续有效。

自本页合入起，后续开发应优先遵循本页列出的 `accepted` 决策、目标架构、实施规格与阶段计划。较早的 APP 化、全离线、管理后台、Hono 迁移、认证迁移等报告仍可用于理解背景，但不得在与本主题权威文档冲突时覆盖当前口径。

文档层级：

- `docs/decisions/` 冻结单点稳定决策；
- `docs/architecture/` 说明长期目标边界；
- `docs/specs/` 定义实现必须满足的规范；
- `docs/plans/` 定义迁移顺序、门禁与回滚；
- 领域专项规格继续约束其业务域；
- `docs/reports/`、较早 `docs/plans/` 与 `docs/logs/` 只作为背景和实施证据。

## 当前执行窗口（2026-08-27）

平台重整在进入 Phase 1 前没有留下足以通过 Phase 0 全部交付物与退出门禁的证据；该缺口属于历史事实，不得用后续 Phase 的成果倒填为当时已经闭合。Phase 2.5A/B/C/D、插入的 G25R 与 G25H-1/2、G25E-1 已依次完成。G25E-2 曾在 `5d6b952e` 关闭当时独立审查问题，并以真实本机 Redis empty drill、built server runtime、完整 workspace/build/boundary gate 和四维独立复审记录为 Phase 2.5 隔离退出；该结论现由下述补充审计更正。root 只保留 workspace 编排、统一门禁与跨 runtime route/migration tooling。默认 Hosted DR routing 已由 2026-08-29 专项改为低成本 `client-preflight`；production stable managed control plane 与真实生产 fault drill 仍明确 `DEFERRED`，不得描述为生产自动容灾。2026-08-30 起 Preview 恢复为 production 前置环境，沿用既有 `hono-preview` GitHub Environment、production runtime 配置副本和 production D1；独立 Preview provisioning/evidence contract 已撤回。下一执行入口在 G3-1 Admin read-only、G4-1 Web local-first、G5A-1 Desktop PoC 与 Arena Room runtime 恢复之间按最新优先级重排。不维护追溯式 Phase 0 inventory、证据快照或生产占位门禁。

2026-08-27 的 default-vs-refactor 用户可感知行为补充审计曾以独立 manifest 冻结 shared/exited API 结构和基础设施文案；2026-09-01 复杂度预算决策已撤回该源码/evidence 门禁。Provider 安全诊断、Arena 取消确认、基础设施文案与 `plain-stream` retirement 仍由各自运行时代码和普通行为测试保护，未覆盖的逐路由差分仅作为历史审计缺口，不再阻断正常 CI 或发布。

生产切流前代码收口已在 `8ea87c37`、`d6b45dc8` 关闭只读生产证据确认的 D1 战报角色明细参数上限、原始错误泄露、Hono Node 浮动镜像和 runtime env 权限门禁缺口；完整本地 CI、OpenNext build 与隔离 Hono/Redis/D1 runtime 验证通过。生产实例没有因该工作发生变更，仍运行 legacy release 布局。较早“stable control plane 未纳管即阻断 production Web build”的口径已被 [低成本 Hosted DR 专项](./2026-08-29_070000_低成本HostedDR与客户端预检切换.md) 取代，并在 `34d79bec` 由新的 client-preflight fail-closed gate 原子替换；它不关闭 Arcane Docker 控制面 `homura-admin.colanns.me` Access/direct-origin bypass blocker，也不等于发生过生产切流。历史验收背景见 [生产部署切流前收口验收](../logs/2026-08-27_132500_生产部署切流前收口验收.md)。

当前分支已在 source commit `e82a20f2` 之后完成 [Phase 2.5A D1 Gateway 应用迁移](../logs/2026-08-23_195610_平台重整Phase2.5A_D1网关应用迁移实施日志.md)：`apps/d1-gateway` 已成为首个拥有真实源码、manifest、test/lint/build、环境说明和手工部署入口的 workspace app，并保持既有 D1 binding、HMAC/Access、Wrangler、health 与回滚语义。

Phase 2.5B 已完成 10 条 Hono shared seam：首个纵切将 `generate-magical-girl` 的请求校验、业务顺序和 wire response 下沉到 `@mahoshojo/hosted-api`，由 Next 与 Hono adapter 共同调用；G25B-1 又以同一 contract 链收口 Game Card、Free generate/stream、Scenario generate/stream 五条常规 Hosted generation route；G25B-2 进一步收口 Creator 与残兽 generate/stream 四条深 composition route。结构退出审计随后裁决其余 14 条 capability 不再保留伪双 runtime，将它们登记为 `exitedRouteIds` 并留在原 Next 公开路径。G25R 在不恢复 legacy dynamic import 的前提下，让 `arena/generate-stream` 经 shared server runtime 精确 re-entry，并增加 generation request lookup/stream/status/cancel 四条必要控制面；G25H-1 又精确归位 `arena/generate`、`generate-battle-story` 与 `arena/session/generate-next`。G25H-2 将 Details / Sublimation 四路的应用服务与 Node composition 下沉到 package，Web 仅保留 DR compatibility adapter。G25E-1 又增加代表性 `GET|HEAD /api/hosted/dr-readiness` safe-read 双入口，当前 registry 为 `23 shared-service / 6 exited / 0 legacy-next`。Arena create 使用稳定 request identity 与单 producer reservation，subscriber 断线不再取消服务器生成；Redis 保存有界 active lifecycle/replay，D1 `battle_report_generations` 只保存真实 Provider 终态与有界 finalization manifest，R2/`large_objects` 保存完整正文。Next/OpenNext 保留同一 shared core 的 DR adapter，但没有共享 Redis 时 active store 显式 fail closed，只能恢复已经完成的 D1/R2 terminal，不能退回进程内 reservation。`regenerate` 仍明确留在 exited/Next。详细证据见 [Phase 2.5B 首条 Hono 共享 Seam 实施日志](../logs/2026-08-23_203508_平台重整Phase2.5B首条Hono共享Seam实施日志.md)、[G25B-1 实施日志](../logs/2026-08-24_003214_平台重整G25B-1常规Hosted生成族收口实施日志.md)、[G25B-2 实施日志](../logs/2026-08-24_045949_平台重整G25B-2Creator与残兽生成族收口实施日志.md)、[Phase 2.5B Hono Seam 结构退出审计](../logs/2026-08-24_060244_平台重整Phase2.5B_HonoSeam结构退出审计.md)、[G25R 实施日志](../logs/2026-08-25_120500_平台重整G25R可恢复Arena生成实施日志.md)、[G25H-1 实施日志](../logs/2026-08-26_102221_平台重整G25H-1Arena伴随AI路由归位实施日志.md) 与 [G25H-2 实施与退出审计](../logs/2026-08-26_160740_平台重整G25H-2_Details与Sublimation双入口实施与退出审计.md)。

G25E-1 的完整设计、提交、验证、独立复审与回滚证据见 [G25E-1 实施日志](../logs/2026-08-26_201133_平台重整G25E-1_Hosted_DR契约与稳定入口实施日志.md)。

提交 `f100413e` 后的复审确认的 finalization 伪终态、initial POST unknown replay、content-policy cancel 与 generation-scoped random metadata 四个缺口，已由 `041edb0a`、`bd38ab81`、`36cd6dd5`、`204bda14` 及后续 `477f6bbe`、`cc9112a4`、`45fee887` 闭合；对应整改计划状态为 `completed`。这不等于本分支执行过 production deploy、入口切流或 credential 变更。

Hono 容量 telemetry 已扩展为固定 schema 单行 JSON：除 process CPU/RSS/heap、event-loop 和 active/peak request/stream/socket 外，还由真实调用 seam 采集 AI attempt/TTFB/duration/终态、D1 round-trip/rows/error、Redis operation/latency/INFO 资源快照，以及 Arena request/resume/replay/snapshot/provider/terminal、D1/R2 phase、cancel、producer-lost 和 generation duration 分位数。Companion 及 Details/Sublimation generation observation 由 runtime composition 注入受信 operation/placement，并记录真实 success/rejected/failure/cancelled 终态与 duration；Cloudflare DR adapter 发出同一低基数字段集的结构化 observation。流式 observation 在 body EOF/cancel/read error 时结算且最多一次。两侧 terminal audit 都不记录 prompt、正文、URL、Provider 或凭据。采样超时不会阻塞主快照。Hono 当前无法观测入口层真实 DR 选择，因此明确记为 `not-observed`，不伪造 `primary` 选择事实；控制面选择/failover reason 仍须由后续可信入口注入。Phase 2.5B 的初始证据见 [Hono Node 容量遥测基础实施日志](../logs/2026-08-23_205520_平台重整Phase2.5B_HonoNode容量遥测基础实施日志.md)，容量收口见 [G25C 实施日志](../logs/2026-08-25_023625_平台重整G25C_apps-api真实应用激活与Telemetry收口实施日志.md)，Arena fault/telemetry 证据见 [G25R 实施日志](../logs/2026-08-25_120500_平台重整G25R可恢复Arena生成实施日志.md)、[G25H-1 实施日志](../logs/2026-08-26_102221_平台重整G25H-1Arena伴随AI路由归位实施日志.md) 与 [G25H-2 退出审计](../logs/2026-08-26_160740_平台重整G25H-2_Details与Sublimation双入口实施与退出审计.md)。

Hono 源码、manifest、测试、bundle、Docker/Compose 与 deploy script 已归属真实 `apps/api` workspace；旧 `server/`、根 Hono Dockerfile 和 `deploy/hono` owner 已退出。Next/OpenNext source、manifest、test/lint/build、static/env/deploy ownership 现由真实 `apps/web` workspace 独占；root compatibility command 只通过 workspace filter 编排，root tooling 若按 alias、相对路径、baseUrl 或 app package name 静态导入 app source 会由 boundary gate 拒绝。G25E-2 机器事实为 `23 shared-service / 6 exited / 0 legacy-next`，显式 deployment target 与 stable-origin activation gate 均 fail closed；隔离 fault matrix、真实本机 Redis empty drill、built server runtime、188 页 Next/OpenNext build、完整 `ci:verify` 与当时四维独立审查均已通过。该证据支持具体技术门禁，但不再被解释为完整 `ACCEPT-014` 或无条件 Phase 2.5 总退出。本 Goal 未执行 production deploy、DNS/LB 切流、远程生产数据写入或 credential 变更；`provisioning=not-provisioned` 仍表示 DR LB/DNS/control-plane 尚未配置。详细证据与更正见 [G25E-2 与 Phase 2.5 退出审计](../logs/2026-08-27_094500_平台重整G25E-2与Phase2.5退出审计.md)。

后续平台关键路径统一为：

```text
Phase 2 通用共享内核收口（已通过退出审计）
  -> Phase 2.5A apps/d1-gateway（已激活）
  -> Phase 2.5B Hono seam（已通过结构退出审计：10 shared / 14 exited / 0 legacy）
  -> Phase 2.5C apps/api + telemetry（已完成）
  -> G25R Arena 可恢复 generation（已完成：15 shared / 13 exited / 0 legacy）
  -> Phase 2.5D apps/web（已完成）
  -> G25H-1 Arena companion routes（已完成：18 shared / 10 exited / 0 legacy）
  -> G25H-2 Details / Sublimation routes（已完成：22 shared / 6 exited / 0 legacy）
  -> G25E-1 Hosted DR contract / stable endpoint（已完成：23 shared / 6 exited / 0 legacy；control plane not-provisioned）
  -> G25E-2 Hosted DR drills 与 Phase 2.5 总退出审计（用户可感知 blocker PASS；ACCEPT-018/014 PARTIAL；生产动作 deferred）
  -> 低成本 Hosted DR client-preflight（仓库实现与本地 fault evidence 完成；production activation deferred）
  -> Admin / Web local-first / Desktop 薄 PoC 并行
  -> Desktop MVP / Arena Room runtime 等后续阶段
```

执行约束：

- Phase 2.5 用户可感知 blocker 已关闭，但完整 `ACCEPT-018` 与 `ACCEPT-014` 仍有证据缺口；后续轨道可以按已接受的并行时序继续，不能把该并行授权写成完整 exit gate 已 PASS。Arena Room WebSocket/ticket/UI runtime 可以按领域 accepted spec 重新估算与排期，但 G25R generation reliability seam 本身不等于这些 runtime 已经完成。
- Hono API 路由面仍不得为“迁移覆盖率”机械扩张；新增 capability 必须提供独立运行时、成本、可靠性、安全或解耦收益，并按 accepted 计划逐族裁决。
- Phase 2.5 同时建立 Hosted DR seam：Hono HK 是主执行面，Cloudflare Next/OpenNext 保留显式 DR manifest 的长期 adapter；Web 对每个新 intent 在 dispatch 前按 readiness 与 manifest 选择一次，dispatch 后不跨 runtime 重放。legacy import/wrapper 要删除，DR adapter 不删除。
- `feat/admin` 不整分支合并；Phase 2.5 后从 read-only 业务域开始迁入 `apps/admin`。
- Desktop 可以在 Phase 2.5 后尽早做最小 Tauri PoC 验证共享边界，但完整 MVP 等 Web local-library/Direct 数据语义稳定后再展开。

当领域实施计划与平台计划在同一分支上产生顺序冲突时，**平台阶段退出门禁决定“何时做”，领域规格继续决定“做成什么语义”**。这项优先级规则不允许借实施顺序修改已冻结的 Arena、认证、数据所有权或服务器权威语义。

## 当前稳定口径

### 1. 本地优先、按需联网

- Desktop/Mobile 在本地能够完成的编辑、卡库、历史、草稿、Direct AI 与本地模型调用，默认不得绕经项目服务器。
- APP 的本地功能不得要求用户先登录。
- 项目服务器只承担确实需要项目资源、全局共享状态或服务器权威的能力。

### 2. 服务器权威边界

- 官方 Hosted 生成、官方原生签名、严格排位、多人权威状态、线上卡库、账号、审核与治理继续由服务器控制。
- 客户端、客户端构建产物和客户端提交的数据均视为不可信输入。
- 本地 Direct 生成不能自行获得官方原生签名，也不能直接进入严格排位。

### 3. Monorepo

仓库迁移为 pnpm workspace。目标独立应用和部署单元为：

```text
apps/
  web/
  admin/
  api/
  d1-gateway/
  arena-room/  # 未来可选 adapter；v1 Hono + Redis 首发不要求创建
  desktop/
  mobile/
```

领域规则、协议、Direct AI、本地库、云端客户端与多人基础设施通过有明确依赖方向的共享包复用。

`apps/*` 只有在成为真实源码/manifest/构建/环境/部署所有者后才算完成应用边界迁移；占位 README 或空壳 manifest 只表示目标位置，不得作为阶段完成证据。

### 3.1 Hosted 运行时容灾

- `apps/api` / 香港 Hono 是 Hosted HTTP 默认主执行面；单台 VPS 不被视为业务事实源。
- `apps/web` / Cloudflare Next/OpenNext 对显式 DR capability/route manifest 提供长期灾备 adapter；两个 runtime 共享同一 service/domain/contracts，不维护两套业务逻辑。
- Web 当前以生成的公开 primary/DR routing 投影执行 per-intent client preflight；未来 Installed Client 可复用同一纯选择 contract。可选 stable managed endpoint 不再是默认前置。
- D1/DO/R2 按领域继续持有权威事实；Redis 只保存可重建或允许丢失的 cache、rate-limit、lease、presence、Pub/Sub/telemetry 等状态。
- Hono 正常走 D1 Gateway；Cloudflare DR 尽量使用独立 D1 binding/Sessions，避免共享同一 Gateway 故障路径。
- safe/read 可以健康路由切换；generation/stream/settlement 等非幂等操作一旦可能开始执行，不得因为连接失败盲目跨 runtime 重放。
- D1 本身故障时不把 Redis 提升为数据库；线上权威能力明确降级，Local/Direct 路径继续可用。

### 4. 管理后台

- `feat/admin` 不再作为长期管理员专用分支。
- 后台迁入独立 `apps/admin`，Cloudflare Access 是入口访问控制；若 origin 为自托管/VPS，再使用 Tunnel 或等价的 origin-bypass 防护。若 Worker 本身就是 origin，可直接使用 Worker/hostname Access，不为满足形式要求强制增加 Tunnel。
- Access 之后仍执行 Origin/JWT（按实际部署形态）、应用层 RBAC 与审计；现有分支中的“管理员本地运行即可信”假设不得原样带入公网部署。

### 5. Direct AI

- 供应商登记簿是便捷预设，不是允许名单。
- Desktop/Mobile 允许用户显式创建自定义供应商、端点、模型和请求参数。
- APP 首期以本地 Direct 为主，可以不提供 Hosted 生成。
- Direct 请求不得携带项目 Cookie、Legacy Bearer、Better Auth session、活动令牌或服务器秘密。

### 6. 本地库与线上库并列

- Web 使用独立 IndexedDB 本地库；Installed APP 使用 SQLite 等适合其运行时的持久存储。
- 本地库不占用线上卡槽。
- 首期只支持显式“保存到本地、上传副本、下载副本、导入、导出、备份/恢复”，不做自动双向同步。
- 存储位置与原生性是两个正交维度。

### 7. 公开仓库与发行

- 服务器密钥、项目 AI Key、签名私钥、数据库凭据和发布私钥不得进入仓库或客户端包。
- Desktop 首选 Tauri 2，Electron 作为经 PoC 触发的后备方案。
- Desktop 通过 GitHub Releases 分发，并使用 Tauri Updater 签名校验更新。
- Android 可通过 GitHub Releases 分发签名 APK，但不得把 Desktop Updater 方案误用于移动端。
- iOS 发行作为独立工作流，不阻塞 Desktop/Android。

### 8. Legacy 兼容范围

Legacy 登录在本轮大重整中保持现状，不改变用户流程、不下线 authkey，也不混入本轮迁移。较早提出的 Legacy 收口与短期令牌设计保留在后续 backlog。

### 9. 旧 PVP

- 旧 PVP 去留与正在推进的 Arena 多人协作不是同一决策。
- 不凭代码规模或主观印象直接删除 PVP；先采集使用、完成率、复玩率、成本与维护负担。
- 若决定下线，先抽取可复用的房间、参与者、版本控制、事件与恢复能力，再移除 PVP 产品层。

### 10. Arena 多人协作

2026-08-21 的 Arena 多人协作 v1 规格与计划继续作为该业务域的权威文档；部署边界由现行 Arena Room 可移植运行时 ADR 补充冻结：

- **产品层不拆 Arena**：多人入口、配置和结果继续整合在现有 `/arena`。
- **运行时可替换**：v1 首发由 `apps/api` Hono + Redis 承载 Room authority adapter；不要求建立 `apps/arena-room` 或 Durable Object，未来 adapter 必须复用同一协议与权威语义。
- **production ingress 复用 Hono primary**：Room HTTP/WSS 使用 Hosted manifest 的 primary origin 与既有 Hono public ingress；preview 使用同 manifest 的 preview origin。`api.mahoshojo.colanns.me` 只属于 optional Hosted stable-control-plane seam，不是 Room v1 provision 前置。
- Room Shared Config 只保存安全白名单投影，不保存 Provider 凭据、完整本地私有卡或战报运行态。
- host-local 完整 payload 不进入 Room plane；权威生成确有需要时，只按既有 generation contract 临时直送 generation service。
- 成员同步后在本地编辑，Proposal 除 `baseRevision` 外还必须有 typed base precondition，服务端/DO 可在不保存无限 revision 历史的情况下判定 stale conflict。
- 生成由房主发起并冻结不可变 snapshot；一次用户意图使用稳定 `generationRequestId`，浏览器/网络重试不得重复生成。
- 房主浏览器不是 AI stream 的房间中继；Hono 首发可在同一服务器边界连接 generation 与 Room，未来跨部署 adapter 才需要明确 bridge。
- reconnect 使用服务器权威 checkpoint/cursor；story delta 是可恢复失败的临时展示流，最终战报以 D1/R2 generation storage 为准。
- Monorepo 迁移可以调整文件路径，但不得静默改变上述语义。
- 实施顺序受平台计划 Phase 2.5 门禁约束：当前已完成的 contract/纯逻辑与 G25R generation 成果保留；完整 Room runtime 在 Phase 2.5 退出后恢复，避免平台重整分支继续被单一功能纵向开发占用。

## 权威文档

| 层级 | 文档 | 用途 |
| --- | --- | --- |
| 决策 | [本地优先与服务器权威边界决策](../decisions/2026-08-22_022100_本地优先与服务器权威边界决策.md) | 冻结执行与信任边界 |
| 决策 | [Monorepo 与应用边界决策](../decisions/2026-08-22_022200_Monorepo与应用边界决策.md) | 冻结仓库和部署单元 |
| 决策 | [Arena Room 运行时可移植与 Hono + Redis 首发决策](../decisions/2026-08-25_104400_ArenaRoom运行时可移植与HonoRedis首发决策.md) | 取代旧 DO-first 部署约束，冻结可移植 Room authority、Hono + Redis 首发、恢复与安全边界 |
| 决策 | [Arena 多人 Redis-only Room Directory 决策](../decisions/2026-08-28_181500_Arena多人RedisOnly目录决策.md) | 部分取代前述 ADR 的 D1 directory 决定；冻结 Redis 原子目录、authority 重验与 v1 不发布 `arena_multiplayer_rooms` |
| 决策 | [Arena 生产 Room 入口复用 Hono Primary 决策](../decisions/2026-08-31_080000_Arena生产Room入口复用HonoPrimary决策.md) | 覆盖 GMR-11 的独立 Room logical-origin 前置；冻结 shared Hono primary ingress、caller/service origin 分离与不新增 DNS/Caddy/TLS |
| 历史决策 | [Arena Room 部署边界与实时权威决策](../decisions/2026-08-22_184800_ArenaRoom部署边界与实时权威决策.md) | 已被现行 Arena Room ADR 取代，仅保留设计历史 |
| 决策 | [Hosted 运行时容灾与 Cloudflare 灾备决策](../decisions/2026-08-23_104000_Hosted运行时容灾与Cloudflare灾备决策.md) | 冻结 Hono 主执行面、Cloudflare DR、数据权威与非幂等 failover 边界 |
| 决策 | [本地库与数据所有权决策](../decisions/2026-08-22_022300_本地库与数据所有权决策.md) | 冻结本地/云端数据语义 |
| 决策 | [客户端运行时与发行决策](../decisions/2026-08-22_022400_客户端运行时与发行决策.md) | 冻结 Desktop、Mobile 与 Release 口径 |
| 架构 | [平台重整目标架构](../architecture/2026-08-22_022500_平台重整目标架构.md) | 长期边界、依赖方向与信任区 |
| 规格 | [平台重整实施规格](../specs/2026-08-22_022600_平台重整实施规格.md) | 可测试的规范性要求 |
| 部分取代的规格 | [AI 错误公共投影与用户可感知契约审计规范](../specs/2026-08-27_200200_AI错误公共投影与用户可感知契约审计规范.md) | Provider 安全诊断与用户故障文案继续有效；default-vs-refactor 源码/evidence merge gate 已退出 |
| 计划 | [平台重整分阶段实施计划](../plans/2026-08-22_022700_平台重整分阶段实施计划.md) | 顺序、门禁、回滚和交付物 |
| 领域规格 | [Arena 多人协作 v1 规格](../specs/2026-08-21_arena-multiplayer-v1-spec.md) | 房间投影、Proposal、权威生成与资源边界 |
| 领域规格修订 | [Arena 多人 v1 Redis-only directory 规范修订](../specs/2026-08-28_150500_Arena多人v1移除D1目录RedisOnly规范修订.md) | 覆盖 ROOMRT-018/GMR-06 D1 目标态；Room checkpoint 与 discovery 统一为 Redis runtime 边界，保留历史实现但不进入 v1 migration/runtime |
| 领域规格修订 | [Arena 生产 Room 入口简化与架构重整规格](../specs/2026-08-31_080100_Arena生产Room入口简化与架构重整规格.md) | 冻结 Room service origin 单一来源、client/server/workflow 收敛、负向 probe 与后续 production activation 门禁 |
| 实施计划 | [Arena 多人 Redis-only Room Directory 清理实施计划](../plans/2026-08-28_154000_Arena多人RedisOnly目录清理实施计划.md) | 按原子 Redis lifecycle、query authority、D1/schema 清理、真实 Redis 与独立复审推进 GMR-06R |
| 实施日志 | [Arena 多人 GMR-06R Redis-only Room Directory 实施与审查整改日志](../logs/2026-08-28_181500_Arena多人GMR06R_RedisOnly目录实施与审查整改日志.md) | 记录代码 checkpoint、远程 D1 只读退出审计、本机 Redis/AOF、workspace 验证与独立复审闭环 |
| 历史归档 | [Arena 多人 GMR-06 D1 Room Directory 实现归档与恢复指南](../logs/2026-08-28_150500_Arena多人GMR06D1目录实现归档与恢复指南.md) | `historical / superseded / reference-only`；保留 commit range、竞态经验与未来显式 ADR 后的恢复路径 |
| 实施设计 | [Arena 多人 GMR-08 Proposal 端到端设计](../specs/2026-08-28_143000_Arena多人GMR-08Proposal端到端设计.md) | 已实现；冻结 Proposal HTTP、DataCardRef authority、viewer projection、unknown/replay 与 UI working-copy 边界 |
| 实施计划 | [Arena 多人 GMR-08 Proposal 端到端实施计划](../plans/2026-08-28_143000_Arena多人GMR-08Proposal端到端实施计划.md) | 已完成；按 contract/core、ref verifier、service/routes、Web/UI、fault/review 原子实施 |
| 实施日志 | [Arena 多人 GMR-08 Proposal 端到端实施与审查整改日志](../logs/2026-08-28_151800_Arena多人GMR-08Proposal端到端实施与审查整改日志.md) | 记录 GMR-08 checkpoint、真实 Redis/WSS、workspace 验证、独立复审与回滚 |
| 实施设计 | [Arena 多人 GMR-09 权威生成发布设计](../specs/2026-08-28_190500_Arena多人GMR-09权威生成发布设计.md) | 已实现；冻结 host-only reservation、shared semantic digest、single producer、Room publisher、terminal/recovery 与 release gate |
| 实施计划 | [Arena 多人 GMR-09 权威生成发布实施计划](../plans/2026-08-28_190500_Arena多人GMR-09权威生成发布实施计划.md) | 已完成；按 contract/core/API/Web、真实 Redis/SIGKILL、rollback gate 与独立复审原子推进 |
| 实施日志 | [Arena 多人 GMR-09 权威生成发布实施与审查整改日志](../logs/2026-08-28_204800_Arena多人GMR-09权威生成发布实施与审查整改日志.md) | 记录权威生成 checkpoints、durable terminal、secret/replay、release tuple、full validation 与三路复审闭环 |
| 实施设计 | [Arena 多人 GMR-10 运行时 Hardening 与退出审计设计](../specs/2026-08-28_235400_Arena多人GMR-10运行时Hardening与退出审计设计.md) | 已实现；冻结 telemetry v5、十类 drill、exact loss/VPS 语义与非生产负载基线边界 |
| 实施计划 | [Arena 多人 GMR-10 运行时 Hardening 与退出审计实施计划](../plans/2026-08-28_235400_Arena多人GMR-10运行时Hardening与退出审计实施计划.md) | 已完成；按 telemetry、component instrumentation、fault evidence、load 与退出复审推进 |
| 实施日志 | [Arena 多人 GMR-10 运行时 Hardening 与退出审查整改日志](../logs/2026-08-29_014921_Arena多人GMR-10运行时Hardening与退出审查整改日志.md) | 记录真实 Hono/Redis/WSS fault/load、exactly-once、full validation、独立 findings 与 v1 停止状态 |
| 部分取代的生产激活计划 | [Arena 多人生产激活与回滚实施计划](../plans/2026-08-30_231000_Arena多人生产激活与回滚实施计划.md) | source review/GMR-11 proof 已退出；feature flag、health、smoke 与 rollback 边界继续有效 |
| 实施日志 | [Arena 多人 GMR-11 激活准备与实时流收尾日志](../logs/2026-08-30_183857_Arena多人GMR-11激活准备与实时流收尾日志.md) | 记录 Preview 激活证据、实时流参数收口、production 只读差距与剩余外部项 |
| 整改设计 | [G25R 可恢复 Arena 生成审查整改设计](../specs/2026-08-25_205143_G25R可恢复Arena生成审查整改设计.md) | 闭合 finalization、unknown POST recovery、content-policy cancel 与随机 metadata 一致性 |
| 整改计划 | [G25R 可恢复 Arena 生成审查整改实施计划](../plans/2026-08-25_205452_G25R可恢复Arena生成审查整改实施计划.md) | 按四个原子工作单元实施、验证与复审 |
| 战后角色更新规格 | [Arena 战后角色更新双信任通道规格](../specs/2026-09-01_133000_Arena战后角色更新双信任通道规格.md) | 保留 G25R 权威对账，并新增只产生 unsigned derivative 的可编辑 repair lane |
| 战后角色更新计划 | [Arena 战后角色可编辑修复实施计划](../plans/2026-09-01_133100_Arena战后角色可编辑修复实施计划.md) | 以 domain patch、AI-only endpoint、客户端确认与安全回归恢复手工/AI 修复 |
| 实施设计 | [G25D apps/web 真实应用激活设计](../specs/2026-08-25_232714_G25D_apps-web真实应用激活设计.md) | 冻结 Web relocation、root ownership、环境兼容与回滚边界 |
| 实施计划 | [G25D apps/web 真实应用激活实施计划](../plans/2026-08-25_232714_G25D_apps-web真实应用激活实施计划.md) | 记录 G25D 原子 checkpoint、验证与独立审查 closure |
| 实施设计 | [G25H-1 Arena 伴随 AI 路由归位设计](../specs/2026-08-26_082507_G25H-1Arena伴随AI路由归位设计.md) | 冻结 typed subscription、companion projector、authority、telemetry 与回滚边界 |
| 实施计划 | [G25H-1 Arena 伴随 AI 路由归位实施计划](../plans/2026-08-26_082507_G25H-1Arena伴随AI路由归位实施计划.md) | 记录 G25H-1 原子 checkpoint、验证与独立审查 closure |
| 整改设计 | [G25H-1 审查整改设计](../specs/2026-08-26_112538_G25H-1审查整改设计.md) | 裁决文章 contract、PVP auditable rejection 与 legacy stats 退休边界 |
| 现行修正规格 | [Arena 双生成契约回归修正规格](../specs/2026-08-31_063734_Arena双生成契约回归修正规格.md) | 取代 G25H-1 的单一文章 contract；恢复 stream Markdown 与 non-stream structured report 的独立 Provider/输出契约，并登记 no-replay、system/BYOK 分层预算、共同基础设施边界与单次解析要求 |
| 整改计划 | [G25H-1 审查整改实施计划](../plans/2026-08-26_113603_G25H-1审查整改实施计划.md) | 记录整改原子单元、验证与复审门禁 |
| 实施日志 | [G25H-1 审查整改实施日志](../logs/2026-08-26_132601_G25H-1审查整改实施日志.md) | 汇总最终实现、命令、warning、回滚与外部操作影响 |
| 领域计划 | [Arena 多人协作 v1 实施计划](../plans/2026-08-21_arena-multiplayer-v1-implementation-plan.md) | 产品/Proposal/generation/security 语义继续有效；DO-first runtime 与 D1 directory 段落已被现行 ADR/spec/计划取代 |

## 与既有文档的关系

### 仍然有效

- `docs/2026-08-22_034220_Hono服务部署与自动发布指南.md`
  - 已随 G25C 更新为 `apps/api` owner、完整 release tuple、旧生产布局纳管、持久事务与回滚手册；实际路由、鉴权、telemetry 和运行时说明从 `apps/api/README.md` 进入。
- `docs/plans/2026-08-21_131924_Hono_D1_Redis后端迁移方案.md`
  - D1 authoritative、Redis non-authoritative、Gateway 与缓存故障策略继续作为设计依据；
  - 其中“稳定后删除旧 Next Route wrapper”的口径只适用于 legacy wrapper/import，不再解释为删除经过 DR manifest/contract test 的长期 Next 灾备 adapter。
- `docs/specs/2026-08-21_arena-multiplayer-v1-spec.md`
  - 继续作为 Arena 多人业务域权威规格。
- `docs/plans/2026-08-21_arena-multiplayer-v1-implementation-plan.md`
  - 继续保存 Arena 多人业务语义与实施历史；DO-first runtime、D1 directory/schema/reconciliation 已被现行
    Hono + Redis 计划和 Redis-only ADR/spec 取代，目录可迁移，保护性语义不得退化。
- Better Auth、Legacy、排位与 PVP 现有报告
  - 继续描述现状和历史决策；本轮不主动改变 Legacy 行为。

### 作为历史背景保留

- `docs/plans/2026-07-29_231448_项目APP化与全离线架构方案.md`
  - 保留调研和方案比较价值；
  - 自本主题合入后，不再作为平台重整的实施权威；
  - 其中“首发 remote、随后 hybrid/standalone”的顺序，由当前 Local-first / Direct-first 口径取代。

## 已延后但有明确门禁的事项

1. **Mobile Runtime 最终选型**
   - 比较 Tauri Mobile 与 Capacitor；
   - 通过 Mobile PoC 门禁后形成单独 ADR。

2. **旧 PVP 去留**
   - 完成生产数据盘点后单独决策；
   - 不影响 Arena 多人协作 v1 继续推进。

3. **APP Hosted 生成**
   - Desktop/Mobile 首期不要求；
   - 只有在用户价值和项目资源预算明确后再设计。

4. **本地库自动同步**
   - 首期明确不做；
   - 等显式复制流程稳定后再评估双向同步和冲突模型。

5. **iOS 发行**
   - 独立评估签名、审核、地区与分发要求；
   - 不阻塞 Web、Desktop 和 Android。

6. **签名算法升级**
   - 当前服务器 HMAC 原生签名继续有效；
   - Key ID、轮换与非对称签名另立规格。

7. **多人 Direct 生成**
   - v1 继续由服务器执行唯一权威生成；
   - 未来若允许房主 Direct 生成后共享，只能作为非权威休闲模式，并需单独 ADR。

## 开发入口

涉及以下范围的 PR，必须在描述中列出对应规格编号并说明符合情况：

- Monorepo 目录或依赖边界；
- Direct AI、自定义 Endpoint 或 API Key；
- 本地库、导入导出或线上卡槽；
- Desktop/Mobile runtime；
- Admin 上线与权限；
- 原生签名、排位、多人与服务器预算；
- Hosted Hono/Cloudflare DR、Redis/D1 authority、failover/replay；
- Arena Room authority / 可选 Worker 或 Durable Object adapter；
- GitHub Release、自动更新或发布密钥。

如实现需要违反当前权威口径，应先新增或更新 ADR，不得只在代码、feature flag 或 PR 评论中临时改变边界。

实施排期必须同时检查平台 plan 与领域 plan：平台 plan 的阶段门禁优先决定跨应用/跨运行时工作的启动时点；领域 plan 不得仅因为自身“下一阶段”已排到 runtime，就跳过尚未闭合的 Monorepo 应用边界。
