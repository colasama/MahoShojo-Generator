# 独立管理端功能迁移与交付记录

日期：2026-09-12。状态：本地代码迁移完成；生产未配置、未迁移、未发布。环境限制项仍待合适运行环境复验。

依据：[实施计划](../plans/2026-09-12_130000_独立线上管理端迁移实施计划.md)、[Admin 安全规格](../specs/2026-08-29_Admin回归安全与迁移规格.md)、[复杂度预算 ADR](../decisions/2026-09-01_193200_平台复杂度预算与故障降级决策.md)。

## 来源和迁移边界

工作分支为 `feat/admin-reintegration`，从主线工作基线 `2c5318e7` 开始；旧功能固定参考 `origin/feat/admin@73cbc27c1aa5c339486d14831e861a28ba21ab39`。只提取适用的业务行为，没有合并旧分支。旧分支从本次迁移后停止承接后台新开发，保留历史 ref；没有删除远程分支、旧表或发布 tag。

旧 `app/admin/[...slug]/page.tsx` 的 22 个页面入口和 `app/api/admin/[...slug]/handler.ts` 的 59 个路径是以下映射的来源。新路由、字段和 capability 的当前事实分别由 `apps/admin/src/app.ts`、`extra-reads.ts`、`actions.ts` 和 `packages/contracts/src/admin.ts` 定义，文档不另建第二套机器路由目录。

网站业务管理端为 `admin.mahoshojo.colanns.me`。`homura-admin.colanns.me` 是 Arcane Docker 管理入口，历史材料将二者等同的推断已纠正。

## 页面去向

| 旧页面 slug | 新工作台去向与处置 |
| --- | --- |
| `users`、`user-dashboard`、`user-management` | 合并为概览、用户列表/安全详情、账号关联摘要；凭据不返回 |
| `user-analytics` | 活跃分析、日快照、最多 30 天历史补齐；历史窗口标注估算 |
| `redemption-codes` | 兑换码查询、生成及废弃未使用码 |
| `badge-management` | 徽章定义、用户持有详情、授予和撤销 |
| `content-management`、`character-management` | 合并数据卡与待审核更新；保留三态可见性、推荐、问卷原生许可、技术指标 |
| `tag-management` | 标签、别名及卡片范围内标签替换 |
| `messages` | 站点消息、定向消息、过期操作 |
| `report-cases` | 举报列表、案件详情和材料、裁决及通知 |
| `report-appeals` | 申诉列表、引用材料、共享状态转换审理 |
| `crowd-review/inspectors` | 巡查员列表与状态 |
| `crowd-review/cases` | 众裁详情、取消、接管、覆盖；阻止晚到结果覆盖人工裁决 |
| `arena-ratings` | 评分列表与受控重置 |
| `arena-rating-events` | 积分事件查询 |
| `arena-risk-audit` | 风险审计查询与汇总 |
| `battle-report-generations` | 生成记录、安全详情、私有正文导出 |
| `pvp` | 旧 PVP 房间/比赛/详情及受控恢复命令 |
| `large-objects` | 大对象 metadata、预览清理与恢复作业 |
| `data-maintenance` | 固定业务目标清理、进度、取消、恢复；不提供任意表操作 |
| `ai-channel-availability` | 渠道只读可用性、快照刷新、到期桶清理 |

新增加的独立页面包括操作审计、作业、AI 建议结果及新 Arena 多人只读观测。所有页面均复用同源 session、URL 筛选、游标分页、显式空态/不可用态和操作确认。版本由详情响应自动绑定；批量操作逐项 CAS，整个请求范围先冻结幂等键。

## API 行为去向

下表省略旧 `/api/admin/` 前缀；旧 HTTP 路径不再作为兼容入口，行为通过版本化资源读取、显式动作注册或私有作业迁移。

| 旧路径 | 新行为 |
| --- | --- |
| `dashboard-stats` | `dashboard` 显式安全计数；缺表/失败返回不可用 |
| `users`、`users/[id]`、`users/batch-update`、`user-accounts` | `users`、`user-accounts` 读取；封禁/免审/卡槽/头衔独立动作与批量动作 |
| `user-analytics`、`user-analytics/snapshot` | 分析摘要和快照动作；每日采集改用同一 Worker cron 的显式管理员委托 |
| `data-cards`、`data-cards/[id]`、`data-cards/batch-update` | 卡片列表/详情，元数据、审核、可见性和推荐动作 |
| `data-card-tags`、`tags`、`tags/[id]`、`tag-aliases` | 标签领域动作；引用中的标签定义停用，避免破坏历史关系 |
| `data-card-updates`、`data-card-updates/batch-review` | 更新审核逐项 CAS；通过时清指标并按当前规则重置严格排位 |
| `data-card-metrics/recompute`、`questionnaire-native` | 共享指标/签名规则和原生许可动作 |
| `ai-channel-availability`、`ai-models`、`ai-review` | 安全模型目录、渠道汇总；持久 AI 建议作业，不自动裁决 |
| `badges`、`badges/[id]`、`badges/grant`、`badges/revoke` | 定义/授予/撤销分别授权、审计及幂等 |
| `redemption-codes` | 安全查询、生成、删除；已兑换记录受保护 |
| `arena-ratings`、`arena-ratings/reset`、`arena-rating-events`、`arena-risk-audit` | 评分域读取及共享规则下的重置 |
| `battle-report-generations`、`battle-report-output`、`export-battle-report-generations`、`export-data-cards` | 生成记录与私有导出作业；正文通过 BFF 重新授权下载，24 小时失效 |
| `pvp` | 拆为窄化读模型和 command；先 CAS 再清理状态，结算结果不确定时拒绝危险重放 |
| `large-objects`、`large-objects/[id]` | 显式 metadata 读取、备份、分步删除和恢复 |
| `data-maintenance/preview`、`data-maintenance/jobs`、`data-maintenance/jobs/[id]`、`data-maintenance/execute` | 固定目标预览、冻结范围、持久作业、Queue 与有界补偿 |
| `messages`、`messages/site`、`messages/direct`、`messages/site/[id]/expire` | 站点/定向消息读取及发布、过期；保留业务 user attribution，新增 admin attribution |
| `report-cases`、`report-cases/[caseId]`、`report-cases/[caseId]/decision`、`report-cases/[caseId]/notify-creator` | 共享案件服务、显式人工裁决和一次性通知 |
| `report-appeals`、`report-appeals/[appealId]`、`report-appeals/[appealId]/review` | 共享申诉服务与版本条件更新 |
| `crowd-review/inspectors`、`crowd-review/inspectors/[userId]/status` | 巡查员安全读模型与状态动作 |
| `crowd-review/cases`、`crowd-review/cases/[roundId]`、`crowd-review/cases/[roundId]/cancel`、`crowd-review/cases/[roundId]/override`、`crowd-review/cases/[roundId]/take-over` | 共享众裁终结条件、人工接管/取消/覆盖和通知去重 |

认证审计、密码重置 token、Better Auth verification、账号关联表只提供必要计数/脱敏摘要，不进入维护执行目标。旧 localhost 信任、任意 SQL、用户整行响应、匿名 catch-all、dry-run 建表和失败后继续删除均退出。

## 数据与部署影响

canonical schema 和纯 Drizzle factory 的机械提取未改变表结构。业务安全基础另有三个增量 migration：`0014_admin_foundation.sql` 建立 principal/operation/audit/job，`0015_admin_actor_attribution.sql` 增加独立管理员关联，`0016_admin_object_tombstones.sql` 防止分步 R2 删除的旧 key 被重新绑定。

Web 的 schema 兼容出口依赖上述新增列。因此在生产部署包含该 schema 的 Web 前，必须先确认 0014–0016 已按既有 migration 流程执行；不要把“只部署 Admin”误解为可跳过共享 D1 迁移。新表是增量保留结构，回滚不删表。原有业务用户外键不填入 Access principal。

新 Arena 内部读取采用独立密钥、固定 audience/action、最长 60 秒服务 JWT。接口只读取现有 Redis 记录，不创建 actor、续 TTL、恢复 checkpoint 或触发生成，不返回房间凭据。该功能需要 Hono 与 Admin 分别配置，默认未启用。

生产 Access、D1/R2/Queue 资源、秘密及部署的具体操作仍须单独确认。操作步骤和恢复验收见 [部署与恢复 runbook](../runbooks/2026-09-12_143000_独立管理端部署与恢复验收.md)。后台新写能力默认关闭，第一次上线按只读验收，后续逐域启用。

## 验证记录

实际完整输出保存在本机忽略目录 `apps/admin/.wrangler/validation/`，不提交可能混入机器路径或环境信息的原始日志。以下为本次实际结果，不以旧规划阶段测试数量代替。

| 检查 | 实际结果 |
| --- | --- |
| `pnpm install --frozen-lockfile` | 通过；14 个 workspace 项目的 lockfile 一致 |
| `pnpm run ci:verify` | 工作区边界、全部 workspace tests/lint/build 通过；仓库级 Hono shell 部署测试 10 项因 Windows 创建 symlink `EPERM` 失败，故整个 CI 未通过 |
| Web / API / hosted-runtime tests | 分别 2424 / 684 / 532 项通过；涉及 schema 的消息/申诉 fixture 已同步 |
| Admin tests | 最后独立复验 13 文件、113 项通过，含本地 Miniflare、验签、恢复、批量与定时授权 |
| workspace lint 与 repo lint | 均通过；本次引入的 PVP 未使用类型告警已移除 |
| repo tests | 146 项通过、10 项环境失败；失败全部位于既有 Hono shell 部署 fixture 的 symlink 创建 |
| API `build:bundle` 与类型构建 | 通过，24 个既有 shared route；新增内部观测由独立窄化路由注册 |
| `pnpm run build:cf` | 最终复验未通过：类型检查、Next 编译与 191 页静态生成通过，OpenNext 所需 standalone 依赖链接阶段报 Windows `EPERM`；需在 Linux/WSL 复验 |
| Admin build / 私有配置示例 dry-run | 通过；Worker 约 2.89 MiB、gzip 561 KiB，浏览器依赖和敏感 binding 边界检查通过 |
| `verify:server:runtime` | 已执行，缺少 `D1_GATEWAY_URL` 而拒绝启动；`REDIS_URL` 也未配置，本机无 Redis/Docker/WSL 可供该真实依赖验证 |
| migration / 故障恢复 | 本地原生 D1 验证 0014–0016、batch 回滚、CAS、幂等、审计失败和唯一管理员恢复；SQLite/R2 合成故障测试覆盖分步恢复、孤儿补偿、撤权、过期及 AI 取消后用量 |
| 浏览器 | 真实浏览器验证审核、筛选分页、版本冲突、断网未知结果防重发、批量修改、恶意文本转义与移动布局；作业取消 UI 另用模拟接口验证 |
| 文档与 Git | 118 个相对链接检查通过，59 个旧 API 路径全部出现在映射；最终执行工作区和基线范围 `git diff --check` |

AI 测试使用合成 Provider，未产生真实付费调用。没有执行生产登录、跨资源备份恢复、真实渠道付费请求或真实 Redis 观测 smoke；这些不由 mock、dry-run 或单元测试替代。

`build:cf` 的两次尝试均在 Next standalone 符号链接创建时报 Windows `EPERM`；最终普通 workspace build 已通过。OpenNext 仍提示 Windows 支持限制和 Web 既有 `compatibility_date=2024-09-23` 偏旧；没有为消除提示而升级生产 runtime。Hono release shell 测试仍需可创建符号链接的 Linux 环境。没有为通过验证而跳过安全断言、关闭生产类型检查或安装系统级运行环境。Windows shutdown 测试通过真实子进程 IPC 触发 process signal event，POSIX 分支仍保留 OS signal，本机未执行 POSIX 分支。

## 提交与上线状态

按机械提取、增量结构、内容/标签、用户权益、消息治理、分析、竞技、作业/AI、工作台和部署文档分为独立提交；另有小型跨平台验证修复。完整清单用 `git log --oneline 2c5318e7..feat/admin-reintegration` 查看；没有 squash、force push、合并旧分支或改写历史。

安全基础和代码可供只读首发审阅。生产下一步是核实新 Access Application、维护者精确邮箱名单、实际绑定和备份，授权执行 migration 与只读发布，再进行 runbook 的真实入口/撤权/deny-all 验收。新增写能力和定时采集均保持默认关闭。
